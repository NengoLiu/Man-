# 服务器端口
PORT=3000

# 数据库主机地址（本地数据库通常为 127.0.0.1 或 localhost）
DB_HOST=localhost

# 数据库用户名（默认通常为 root）
DB_USER=root

# 数据库密码（替换为你的实际数据库密码）
DB_PASSWORD=yourpassword

# 数据库名称（自定义为 man_app）
DB_NAME=man_app

# JWT 密钥（用于加密和解密 token）
JWT_SECRET=replace_with_a_long_random_string  # 替换为长随机字符串